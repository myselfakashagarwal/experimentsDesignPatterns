name: Publish Maven Package

on:
  push:
    branches:
      - legacy
  release:
    types: [created]
  workflow_dispatch:

jobs:
  build-and-publish-base:
    name: Build, Test and Publish to GitHub Packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          server-id: github 

      - name: Build the base module 
        run: mvn -B clean package --file ./base/pom.xml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy Base to GitHub Packages
        run: mvn -B deploy --file ./base/pom.xml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-and-publish-core:
    name: Build, Test and Publish Core to GitHub Packages
    runs-on: ubuntu-latest
    needs: build-and-publish-base
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          server-id: github 

      - name: Install Base Module
        run: mvn -B install --file ./base/pom.xml -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Core Module
        run: mvn -B clean package --file ./core/pom.xml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy Core to GitHub Packages
        run: mvn -B deploy --file ./core/pom.xml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}